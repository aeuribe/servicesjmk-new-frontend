### Satoshi Variable font

To enable the **Satoshi Variable** font, add this file:

- `public/fonts/Satoshi-Variable.woff2`

The project already includes the `@font-face` wiring in `src/app/[locale]/globals.css` via the `font-satoshi` class.


